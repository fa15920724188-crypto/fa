# 地毯AI智能销售资料库：固定网址部署说明

你现在需要的不是临时网址，而是正式部署：

```text
固定网址 + 员工账号密码登录 + DeepSeek API 后端调用
```

## 推荐方案

### 方案 A：最快上线，适合先测试

使用 Render / Railway 这类托管平台。

优点：
- 很快有一个固定网址
- 不需要自己维护服务器
- 适合先给员工测试

缺点：
- 国内访问速度不一定稳定
- 以后正式用可能还是要换成自己的域名

### 方案 B：更适合国内员工长期使用

使用阿里云 / 腾讯云轻量服务器。

优点：
- 国内访问稳定
- 可以绑定自己的域名
- 更适合长期给员工使用

缺点：
- 需要购买服务器
- 需要配置域名和 HTTPS

## 需要准备的东西

```text
1. DeepSeek API Key
2. 员工账号名单
3. 一个固定部署平台：Render/Railway/阿里云/腾讯云
4. 如果要品牌化，还需要域名，比如 ai.yourdomain.com
```

## 环境变量

部署时填写：

```text
PORT=8787
SESSION_SECRET=一串很长的随机字符
AI_PROVIDER=deepseek
DEEPSEEK_API_KEY=你的 DeepSeek API Key
DEEPSEEK_MODEL=deepseek-v4-flash
USERS_JSON=[{"username":"admin","password":"你的密码","name":"Admin"},{"username":"sales01","password":"员工密码","name":"Sales 01"}]
```

## 启动命令

```text
npm start
```

## 注意

- 不要让员工在网页里输入 DeepSeek Key。正式版应该放在服务器环境变量里。
- 员工只需要账号密码登录。
- 正式使用前必须修改默认密码。
- 临时 trycloudflare 网址不适合给员工长期使用。
